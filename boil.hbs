{
  "sitemap": {
    "template": {{{JSONstringify (read "template.hbs")}}},
    "data": {
        "urls": [{{#each (read (fileset files "files"))}}
            {
                "loc": "{{../prefix}}{{{filename}}}",
                "lastmod": {{{JSONstringify modified}}},
                "changefreq": "daily"
            }{{#unless @last}},{{/unless}}
        {{/each}}]
    }
  }
}
